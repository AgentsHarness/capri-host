name: release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      fe_ref:
        description: 'capri-fe 要构建的 ref，默认 main'
        required: false
        default: 'main'

permissions:
  contents: write

jobs:
  frontend:
    name: Build frontend (capri-fe)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout capri-fe
        uses: actions/checkout@v5
        with:
          # Same GitHub owner as this repo: a fork builds that owner's
          # capri-fe, upstream still builds AgentsHarness/capri-fe.
          repository: ${{ github.repository_owner }}/capri-fe
          ref: ${{ inputs.fe_ref || 'main' }}
          path: capri-fe

      # capri-fe 的 package.json 声明了 packageManager: pnpm@<版本>，
      # action-setup 会自动读取该字段，无需在此重复固定版本。
      # Explicit pnpm version: fork's capri-fe may lack packageManager /
      # pnpm-lock.yaml (still on npm). Prefer pnpm when lockfile exists.
      - uses: pnpm/action-setup@v4
        with:
          version: 11.22.0
          package_json_file: capri-fe/package.json

      - uses: actions/setup-node@v5
        with:
          node-version: '24'

      - name: Install deps
        working-directory: capri-fe
        run: |
          if [ -f pnpm-lock.yaml ]; then
            pnpm install --frozen-lockfile
          elif [ -f package-lock.json ]; then
            npm ci
          else
            pnpm install
          fi

      - name: Build frontend
        working-directory: capri-fe
        run: |
          if [ -f pnpm-lock.yaml ] || [ ! -f package-lock.json ]; then
            pnpm run build
          else
            npm run build
          fi

      - name: Upload dist
        uses: actions/upload-artifact@v6
        with:
          name: capri-fe-dist
          path: capri-fe/dist

  build:
    name: build (${{ matrix.goos }}-${{ matrix.goarch }})
    needs: frontend
    strategy:
      fail-fast: false
      matrix:
        include:
          - goos: darwin
            goarch: amd64
          - goos: darwin
            goarch: arm64
          - goos: linux
            goarch: amd64
          - goos: linux
            goarch: arm64
          - goos: windows
            goarch: amd64
          - goos: windows
            goarch: arm64
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      # internal/server/web/dist 被 .gitignore 忽略、不在仓库里，
      # 必须先把前端构建产物放进去，go:embed web/dist 才能编译。
      - name: Install frontend dist into embed dir
        uses: actions/download-artifact@v7
        with:
          name: capri-fe-dist
          path: internal/server/web/dist

      - uses: actions/setup-go@v6
        with:
          go-version: '1.26'
          cache: true

      - name: Build capri-host
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: '0'
        run: |
          ext=""
          gui=""
          if [ "$GOOS" = "windows" ]; then
            ext=".exe"
            # Windows 版是双击运行的：不带 -H=windowsgui 的二进制是 console
            # 子系统，Explorer 会替它开一个终端窗口，而托盘存在的前提正是
            # 没有那个窗口。同时它让 stderr 失效，所以日志走 logs/host.log。
            gui=" -H=windowsgui"
          fi
          mkdir -p dist
          go build -trimpath -ldflags "-s -w${gui} -X github.com/AgentsHarness/capri-host/internal/acp.Version=${GITHUB_REF_NAME}" -o "dist/capri-host-${GOOS}-${GOARCH}${ext}" ./cmd/capri-host

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: capri-host-${{ matrix.goos }}-${{ matrix.goarch }}
          path: dist/capri-host-${{ matrix.goos }}-${{ matrix.goarch }}${{ matrix.goos == 'windows' && '.exe' || '' }}
          if-no-files-found: error

  macos-app:
    name: macOS app
    needs: frontend
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install frontend dist into embed dir
        uses: actions/download-artifact@v7
        with:
          name: capri-fe-dist
          path: internal/server/web/dist

      - uses: actions/setup-go@v6
        with:
          go-version: '1.26'
          cache: true

      - name: Build Capri.app
        env:
          VERSION: ${{ github.ref_name }}
        run: bash packaging/macos/make-app.sh --universal

      - name: Build dmg
        run: bash packaging/macos/make-dmg.sh

      - name: Upload app zip
        uses: actions/upload-artifact@v6
        with:
          name: capri-macos-app
          path: |
            dist/Capri-macos.zip
            dist/Capri-macos.dmg
          if-no-files-found: error

  release:
    name: Create GitHub Release
    needs: [build, macos-app]
    runs-on: ubuntu-latest
    steps:
      # 只下载编译出的二进制，不要包含前端 dist（capri-fe-dist）
      # merge-multiple 让它们平铺到 dist/，再整体上传。
      - name: Download compiled binaries
        uses: actions/download-artifact@v7
        with:
          path: dist
          pattern: capri-host-*
          merge-multiple: true

      - name: Download macOS app
        uses: actions/download-artifact@v7
        with:
          name: capri-macos-app
          path: dist

      - name: Show built files
        run: find dist -type f | sort

      - name: Create Release
        uses: softprops/action-gh-release@v3
        with:
          name: capri-host ${{ github.ref_name }}
          draft: false
          generate_release_notes: true
          files: dist/*
